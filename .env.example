# Host ports for the Docker stacks. Copy to .env to override.

# docker-compose.yml (production build, nginx-served frontends)
BACKEND_PORT=3000
ADMIN_PORT=8080
WORKER_PORT=8081

# docker-compose.dev.yml (Vite dev servers with hot reload)
ADMIN_DEV_PORT=5173
WORKER_DEV_PORT=5174
